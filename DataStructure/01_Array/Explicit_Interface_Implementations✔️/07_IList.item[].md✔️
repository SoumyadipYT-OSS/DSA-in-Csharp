# IList.Item[Int32] Property for Array Class in C# 12

## 📖 Definition
The `IList.Item[Int32]` property is an explicit interface member implementation for arrays in C#. This property gets or sets the element at the specified index in the `IList`. When using this property with arrays, it allows you to access or modify elements at a specific index.

## 🔄 Overloads
The `IList.Item[Int32]` property does not have overloads. It is a single property that gets or sets an element at a specified index.

## 💡 Example
Here is an example demonstrating the behavior of the `IList.Item[Int32]` property in C# 12:

```csharp
using System;
using System.Collections;

class Program {
    static void Main(string[] args) {
        // Create and initialize an array
        int[] numbers = { 1, 2, 3, 4, 5 };

        // Cast the array to IList to access the Item property
        IList list = (IList)numbers;

        // Access elements using the Item property
        Console.WriteLine("Element at index 2: " + list[2]); // Output: 3

        // Modify an element using the Item property
        list[2] = 6;
        Console.WriteLine("Element at index 2 after modification: " + list[2]); // Output: 6
    }
}
```

🎉 Happy Coding! 😊